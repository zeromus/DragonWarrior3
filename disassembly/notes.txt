ЧТО ИСПРАВИТЬ

1. Для перемещаемой таблицы функций и офсетов дальних вызовов надо учесть шорткаты из СРАМы с прямым доступом к функциям _b0_s2-_b0_s3B (индексы в таблице sys1 c $23 по $5C). В банке Е дополнительно используется общая функция _sram_bank0_select_ex для вызова те же процедур по индексу с передачей через регистр, каждый индекс там должен быть скорректирован на idx-$23

НОВЫЕ БАГИ

